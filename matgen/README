This directory contains source code for a simple matrix generator.
The random number generator used is the new C++11 generators, so
your compiler must support #include <random>.

The Makefile targets correspond to the precision to use:

   float, double, ldouble (long double) use the built-in types.

See matgen --help for available options.

